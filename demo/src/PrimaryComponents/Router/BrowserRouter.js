import React from "react";

export const BrowserRouter = () => {
  return (
    <div>
      <h2>Browser Router</h2>
      <ul>
        <li>
          Router that uses <strong>HTML5 history API</strong>
        </li>
        <li>uses regular URL paths</li>
        <li>requires server to be configured correctly</li>
      </ul>
    </div>
  );
};
